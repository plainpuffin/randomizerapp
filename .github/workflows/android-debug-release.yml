name: Android Debug Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-debug-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.7
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Assemble debug APK
        run: ./gradlew :app:assembleDebug

      - name: Upload debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: randomizerapp-debug-apk
          path: app/build/outputs/apk/debug/randomizer-debug.apk
          if-no-files-found: error

      - name: Publish debug prerelease
        uses: ncipollo/release-action@v1
        with:
          tag: randomizerapp-debug-latest
          name: Randomizer App Debug APK
          body: |
            Latest informal debug APK build for Randomizer App.

            This release is updated automatically from the `main` branch workflow.
          prerelease: true
          allowUpdates: true
          replacesArtifacts: true
          artifacts: app/build/outputs/apk/debug/randomizer-debug.apk
